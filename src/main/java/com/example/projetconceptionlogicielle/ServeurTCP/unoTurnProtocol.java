package com.example.projetconceptionlogicielle.ServeurTCP;

import java.io.InputStream;
import java.io.OutputStream;

/**
 * Class used by {@link com.example.projetconceptionlogicielle.UNO_game} package to communicate in a turn.
 */
public class unoTurnProtocol implements IProtocole{
    @Override
    public void execute(IContext aContext, InputStream anInputStream, OutputStream anOutputStream) {

    }
}
